rules:
  - No hardcoded secrets. Reject code that embeds literal API keys or passwords.
  - Require parameterized queries. Reject string-concatenated SQL.
  - Require input validation. Reject handlers that trust unvalidated request fields.
risk_threshold: 100
max_blocked_attempts: 3
max_rounds: 3
